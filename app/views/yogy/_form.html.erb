<% if !@yogy.errors.empty? %>
  <div class="alert alert-error">
    <ul>
      <% @yogy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="well">
  <%= form_for @yogy, html: { multipart: true } do |f| %>
    <%= f.label :name %>
    <%= f.text_field :title %>
    <%= f.label :image %>
    <%= f.file_field :image %>
    <%= f.submit "Save", class: "btn btn-primary" %>
  <% end %>
</div>
